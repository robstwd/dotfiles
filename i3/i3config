# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!
# https://i3wm.org/docs/refcard.html
# https://i3wm.org/i3status/manpage.html
# https://i3wm.org/i3lock/

# Mod1 is Alt key, Mod4 is Win key
set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:DejaVu Sans Mono 11

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

bindsym $mod+Shift+q kill

# start dmenu (a program launcher)
#~ bindsym $mod+d exec dmenu_run
#~ bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# change focus; vim like keybindings
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window; vim like keybindings
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+Control+h split h
bindsym $mod+Control+Right split h

# split in vertical orientation
bindsym $mod+Control+v split v
bindsym $mod+Control+Down split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split
# Toggle between stacking/tabbed/splith/splitv:
bindsym $mod+x layout toggle all

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
bindsym $mod+d focus child

# reload the configuration file
bindsym $mod+Shift+c reload

# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart

# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# The middle button over a titlebar kills the window
bindsym --release button2 kill

# The middle button and a modifer over any part of the window kills the window
bindsym --whole-window $mod+button2 kill

# The right button toggles floating
#~ bindsym button3 floating toggle
bindsym $mod+button3 floating toggle

# ======================================================================
# BAR SETTINGS

# bar for HDMI monitor
bar {
        output HDMI-0
        position top
        separator_symbol " | "
        font pango:monospace 12
        status_command i3blocks -c ~/.config/i3blocks/i3blocks_hdmi.conf
        tray_output primary
        colors {
            background #000000
            separator  #666666
            statusline #ffffff

            focused_workspace  #000ac9 #000ac9 #ffffff
            active_workspace   #4c7899 #4c7899 #ffffff
            inactive_workspace #333333 #222222 #888888
            urgent_workspace   #2f343a #900000 #ffffff
        }
}

# bar for DVI monitor
bar {
        output DVI-I-0
        separator_symbol " | "
        position top
        font pango:monospace 11
        #~ status_command i3status
        status_command i3blocks -c ~/.config/i3blocks/i3blocks_dvi.conf
        colors {
            background #000000
            separator  #666666
            statusline #ffffff

            focused_workspace  #000ac9 #000ac9 #ffffff
            active_workspace   #4c7899 #4c7899 #ffffff
            inactive_workspace #333333 #222222 #888888
            urgent_workspace   #2f343a #900000 #ffffff
        }
}



# ======================================================================
# APPLICATIONS; bound to alt key

bindsym Mod1+a exec asunder
bindsym Mod1+b exec dbus-launch brasero
bindsym Mod1+c exec chromium
bindsym Mod1+d exec dmenu_run
bindsym Mod1+e exec easytag
bindsym Mod1+f exec thunar
bindsym Mod1+g exec geany
bindsym Mod1+h exec ghb
bindsym Mod1+i exec mirage
bindsym Mod1+j exec
bindsym Mod1+k exec kodi
bindsym Mod1+l exec leafpad
bindsym Mod1+m exec meld
bindsym Mod1+n exec nitrogen
bindsym Mod1+o exec libreoffice
bindsym Mod1+p exec keepassx
bindsym Mod1+q exec
bindsym Mod1+r exec remmina
bindsym Mod1+s exec stellarium
bindsym Mod1+t exec totem
bindsym Mod1+u exec
bindsym Mod1+v exec vlc
bindsym Mod1+w exec inox
bindsym Mod1+x exec
bindsym Mod1+y exec
bindsym Mod1+z exec urxvtc

# kill focused window
bindsym Mod1+Escape kill

# ======================================================================
# APPLICATIONS; bound to alt + shift key

bindsym Mod1+Shift+a exec
bindsym Mod1+Shift+b exec
bindsym Mod1+Shift+c exec clipgrab
bindsym Mod1+Shift+d exec digikam
bindsym Mod1+Shift+e exec google-earth
bindsym Mod1+Shift+f exec filezilla
bindsym Mod1+Shift+g exec gramps
bindsym Mod1+Shift+h exec hugin
bindsym Mod1+Shift+i exec
bindsym Mod1+Shift+j exec
bindsym Mod1+Shift+k exec klavaro
bindsym Mod1+Shift+l exec
bindsym Mod1+Shift+m exec midori
bindsym Mod1+Shift+n exec
bindsym Mod1+Shift+o exec
bindsym Mod1+Shift+p exec pavucontrol
bindsym Mod1+Shift+q exec
bindsym Mod1+Shift+r exec
bindsym Mod1+Shift+s exec sftpman-gtk
bindsym Mod1+Shift+t exec
bindsym Mod1+Shift+u exec
bindsym Mod1+Shift+v exec virtualbox
bindsym Mod1+Shift+w exec webcamoid
bindsym Mod1+Shift+x exec
bindsym Mod1+Shift+y exec
bindsym Mod1+Shift+z exec

# ======================================================================
# GAMES; bound to alt + control key

bindsym Mod1+Control+a exec
bindsym Mod1+Control+b exec
bindsym Mod1+Control+c exec
bindsym Mod1+Control+d exec
bindsym Mod1+Control+e exec
bindsym Mod1+Control+f exec
bindsym Mod1+Control+g exec
bindsym Mod1+Control+h exec
bindsym Mod1+Control+i exec
bindsym Mod1+Control+j exec
bindsym Mod1+Control+k exec kshisen
bindsym Mod1+Control+l exec
bindsym Mod1+Control+m exec kmahjongg
bindsym Mod1+Control+n exec neverball
bindsym Mod1+Control+o exec
bindsym Mod1+Control+p exec kpat
bindsym Mod1+Control+q exec
bindsym Mod1+Control+r exec
bindsym Mod1+Control+s exec gnome-sudoku
bindsym Mod1+Control+t exec
bindsym Mod1+Control+u exec
bindsym Mod1+Control+v exec
bindsym Mod1+Control+w exec
bindsym Mod1+Control+x exec
bindsym Mod1+Control+y exec
bindsym Mod1+Control+z exec

# ======================================================================
# others

bindsym Control+Escape exec --no-startup-id rofi -show window -hide-scrollbar -fullscreen

# ======================================================================
# FUNCTION KEYS

#~ bindsym F1 exec
# i3-dmenu-desktop which only displays applications shipping a .desktop file.
bindsym F2 exec --no-startup-id i3-dmenu-desktop --dmenu='rofi -dmenu -hide-scrollbar -fullscreen'
#~ bindsym F3 exec
#~ bindsym F4 exec
#~ bindsym F5 exec
#~ bindsym F6 exec
#~ bindsym F7 exec
#~ bindsym F8 exec
#~ bindsym F9 exec
bindsym F10 exec passmenu
bindsym F11 exec scratchpad show
#~ bindsym F12 exec

# ======================================================================
# FUNCTION FN KEYS

bindsym XF86HomePage exec 
bindsym XF86Mail exec 
bindsym XF86Search exec 
bindsym XF86Calculator exec kcalc
bindsym XF86Tools exec 
bindsym XF86AudioPrev exec 
bindsym XF86AudioPlay exec 
bindsym XF86AudioNext exec 
bindsym XF86AudioMute "exec amixer set Master toggle && pkill -RTMIN+10 i3blocks"
bindsym XF86AudioLowerVolume exec "amixer set Master 5%- && pkill -RTMIN+10 i3blocks"
bindsym XF86AudioRaiseVolume exec "amixer set Master 5%+ && pkill -RTMIN+10 i3blocks"
bindsym XF86Sleep exec --no-startup-id "i3lock --color=000000"

# ======================================================================
# APPLICATION RULES

for_window [class="Vlc"] floating enable
for_window [class="Nitrogen"] floating enable
#~ assign [class="Leafpad"] 2

# ======================================================================
# WORKSPACES

# assign workspaces to particular monitors
workspace 1 output DVI-I-0
workspace 2 output DVI-I-0
workspace 3 output DVI-I-0
workspace 4 output DVI-I-0
workspace 5 output DVI-I-0

workspace 6 output HDMI-0
workspace 7 output HDMI-0
workspace 8 output HDMI-0
workspace 9 output HDMI-0
workspace 0 output HDMI-0

# automatic back-and-forth when switching to the current workspace
workspace_auto_back_and_forth yes

# cycle through workspaces on current monitor
bindsym Control+Tab workspace next_on_output

# switch to workspace
bindsym $mod+1 workspace 1
bindsym $mod+2 workspace 2
bindsym $mod+3 workspace 3
bindsym $mod+4 workspace 4
bindsym $mod+5 workspace 5
bindsym $mod+6 workspace 6
bindsym $mod+7 workspace 7
bindsym $mod+8 workspace 8
bindsym $mod+9 workspace 9
bindsym $mod+0 workspace 10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace 1
bindsym $mod+Shift+2 move container to workspace 2
bindsym $mod+Shift+3 move container to workspace 3
bindsym $mod+Shift+4 move container to workspace 4
bindsym $mod+Shift+5 move container to workspace 5
bindsym $mod+Shift+6 move container to workspace 6
bindsym $mod+Shift+7 move container to workspace 7
bindsym $mod+Shift+8 move container to workspace 8
bindsym $mod+Shift+9 move container to workspace 9
bindsym $mod+Shift+0 move container to workspace 10

# ======================================================================
# COLOURS

# class                 border  backgr. text    indicator child_border
client.focused          #000ac9 #000ac9 #ffffff #2e9ef4   #285577
client.focused_inactive #333333 #4c7899 #ffffff #484e50   #5f676a
client.unfocused        #333333 #222222 #888888 #292d2e   #222222
client.urgent           #2f343a #900000 #ffffff #900000   #900000
client.placeholder      #000000 #0c0c0c #ffffff #000000   #0c0c0c
client.background       #ffffff

# ======================================================================
# binding for i3blocks
bindsym --release Caps_Lock exec pkill -SIGRTMIN+11 i3blocks
bindsym --release Num_Lock  exec pkill -SIGRTMIN+11 i3blocks
